<div class="container">

 
<h1>*****<u>Listing properties</u>*****</h1>
 
<table border="5px">
  <tr>
    <th>Name</th>
    <th>Prize</th>
   
    <th  colspan="3">Buttons</th>
  </tr>
 
  <% @properties.each do |property| %>
    <tr>
      <td><%= property.name %></td>
      <td><%= property.prize %></td>
       <td><%= link_to 'Destroy', property_path(property),method: :delete,data: { confirm: "Are you sure?" }, method: :delete %></td>
      <td><%= link_to 'Show', property_path(property) %></td>
   
    </td>
      </tr>
  <% end %>
</table>
<%= link_to 'Add new property', new_property_path %>
</div>
</div>
